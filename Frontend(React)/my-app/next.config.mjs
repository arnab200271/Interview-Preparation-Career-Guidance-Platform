/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: true, // Disabled — causes "Expected static flag was missing" crash
  //   with babel-plugin-react-compiler@1.0.0 + React 19 (experimental, not production-ready)
};

export default nextConfig;
